<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Amwell - Emergency Medical Services</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --bs-primary: #7AB2D3;
            --bs-secondary: #4A628A;
            --bs-primary-rgb: 122, 178, 211;
            --bs-secondary-rgb: 74, 98, 138;
            --status-online: #6BCBEA;
            --status-busy: #ECC94B;
            --status-offline: #F28C8C;
        }

        body {
            font-family: 'Manrope', sans-serif;
            background-color: #f5f7fa;
            overflow-x: hidden;
        }

        .navbar {
            background: var(--bs-secondary);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.08);
            padding: 1rem 0;
            position: fixed;
            width: 100%;
            z-index: 1000;
            transition: transform 0.3s ease;
        }

        .navbar.scrolled {
            transform: translateY(-10px);
        }

        .navbar-brand {
            font-size: 2rem;
            font-weight: 800;
            color: var(--bs-primary);
            transition: transform 0.3s ease;
        }

        .navbar-brand:hover {
            transform: scale(1.05);
            color: #ffffff;
        }

        .user-profile-img {
            width: 2.5rem;
            height: 2.5rem;
            object-fit: cover;
            border: 2px solid #ffffff;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }

        .user-profile-img:hover {
            transform: rotate(360deg);
        }

        .nav-link {
            color: #ffffff;
            font-weight: 600;
            transition: color 0.3s ease, transform 0.3s ease;
        }

        .nav-link:hover {
            color: var(--bs-primary);
            transform: translateY(-2px);
        }

        .emergency-btn {
            background-color: var(--bs-primary);
            color: #ffffff;
            padding: 0.75rem 2rem;
            border-radius: 50rem;
            font-weight: 600;
            transition: all 0.3s ease;
            animation: pulse 2s infinite;
            box-shadow: 0 4px 15px rgba(122, 178, 211, 0.4);
            border: none;
        }

        .emergency-btn:hover {
            background-color: var(--bs-secondary);
            transform: scale(1.1);
            box-shadow: 0 6px 20px rgba(74, 98, 138, 0.6);
        }

        .header-section {
            margin-top: 100px;
            padding: 3rem 0;
            text-align: center;
            background: linear-gradient(45deg, rgba(122, 178, 211, 0.1), rgba(74, 98, 138, 0.1));
            border-radius: 2rem;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.05);
            margin-bottom: 3rem;
            animation: fadeIn 1s ease-in;
        }

        .header-section h1 {
            font-size: 3rem;
            font-weight: 800;
            color: var(--bs-secondary);
            margin-bottom: 1rem;
        }

        .header-section p {
            font-size: 1.25rem;
            color: #4b5563;
            max-width: 600px;
            margin: 0 auto;
        }

        .ambulance-card {
            border: none;
            border-radius: 2rem;
            transition: all 0.4s ease;
            background: #ffffff;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 0;
            position: relative;
            animation: slideUp 0.5s ease-in;
        }

        .ambulance-card:hover {
            transform: translateY(-1rem);
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.2);
        }

        .ambulance-image {
            width: 100%;
            height: 220px;
            object-fit: cover;
            border-top-left-radius: 2rem;
            border-top-right-radius: 2rem;
            transition: transform 0.3s ease;
            loading: lazy;
        }

        .ambulance-card:hover .ambulance-image {
            transform: scale(1.05);
        }

        .ambulance-details {
            color: var(--bs-secondary);
            font-size: 1rem;
            padding: 1.75rem;
            text-align: left;
            width: 100%;
        }

        .ambulance-details div {
            margin-bottom: 1rem;
            display: flex;
            align-items: center;
        }

        .ambulance-details i {
            color: var(--bs-primary);
            margin-right: 0.5rem;
        }

        .ambulance-details strong {
            font-weight: 700;
            color: var(--bs-secondary);
        }

        .status {
            padding: 0.5rem 1.25rem;
            border-radius: 2rem;
            font-weight: 600;
            font-size: 0.95rem;
            display: inline-block;
            margin-top: 0.5rem;
            transition: transform 0.3s ease;
        }

        .status:hover {
            transform: scale(1.1);
        }

        .status-online {
            background-color: var(--status-online);
            color: #ffffff;
        }

        .status-busy {
            background-color: var(--status-busy);
            color: #2D3748;
        }

        .status-offline {
            background-color: var(--status-offline);
            color: #ffffff;
        }

        .btn {
            padding: 0.75rem 2rem;
            background: linear-gradient(45deg, var(--bs-primary), var(--bs-secondary));
            color: #ffffff;
            border: none;
            border-radius: 50rem;
            font-size: 1.1rem;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.3s ease;
            display: inline-block;
            margin: 0.75rem 0 1.5rem;
            align-self: center;
            box-shadow: 0 4px 15px rgba(122, 178, 211, 0.4);
        }

        .btn:hover {
            background: linear-gradient(45deg, var(--bs-secondary), var(--bs-primary));
            transform: scale(1.1);
            box-shadow: 0 6px 20px rgba(74, 98, 138, 0.6);
        }

        footer {
            background: linear-gradient(180deg, var(--bs-secondary), var(--bs-primary));
            color: #ffffff;
            padding: 6rem 0 3rem;
            position: relative;
        }

        footer a {
            color: #e6f0fa;
            text-decoration: none;
            transition: color 0.3s ease, transform 0.3s ease;
        }

        footer a:hover {
            color: #ffffff;
            transform: translateX(5px);
        }

        .social-icons a {
            font-size: 1.5rem;
            margin: 0 0.75rem;
            transition: transform 0.3s ease;
        }

        .social-icons a:hover {
            transform: scale(1.2);
        }

        .back-to-top {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            background: var(--bs-primary);
            color: #ffffff;
            width: 3rem;
            height: 3rem;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            transition: all 0.3s ease;
            opacity: 0;
            pointer-events: none;
        }

        .back-to-top.show {
            opacity: 1;
            pointer-events: auto;
        }

        .back-to-top:hover {
            transform: scale(1.1);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
        }

        @keyframes pulse {
            0% { box-shadow: 0 0 0 0 rgba(122, 178, 211, 0.4); }
            70% { box-shadow: 0 0 0 20px rgba(122, 178, 211, 0); }
            100% { box-shadow: 0 0 0 0 rgba(122, 178, 211, 0); }
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes slideUp {
            from { opacity: 0; transform: translateY(50px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @media (max-width: 768px) {
            .header-section {
                padding: 2rem 0;
                margin-top: 80px;
            }

            .header-section h1 {
                font-size: 2.25rem;
            }

            .header-section p {
                font-size: 1rem;
            }

            .ambulance-image {
                height: 160px;
            }

            .ambulance-details {
                font-size: 0.95rem;
                padding: 1.25rem;
            }

            .btn {
                padding: 0.6rem 1.5rem;
                font-size: 1rem;
            }

            .back-to-top {
                width: 2.5rem;
                height: 2.5rem;
                bottom: 1.5rem;
                right: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-heartbeat me-2"></i>Amwell
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto align-items-center">
                    <% if (locals.user) { %>
                        <li class="nav-item">
                            <a href="/user/ambulances" class="nav-link emergency-btn">
                                <i class="fas fa-ambulance me-2"></i>Emergency
                            </a>
                        </li>
                        <li class="nav-item dropdown ms-3">
                            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown">
                                <img src="<%= user.profileImage || 'https://via.placeholder.com/40' %>" alt="User Profile" class="user-profile-img rounded-circle me-2">
                                <span class="fw-medium"><%= user.FullName %></span>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li><a class="dropdown-item" href="/user/profile">Profile</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/user/logout">Logout</a></li>
                            </ul>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link btn bg-primary text-white px-4 py-2 rounded-pill fw-semibold" href="/user/signin">Login</a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Header Section -->
    <section class="header-section">
        <div class="container">
            <h1>🚑 Ambulances Near You</h1>
            <p>Connect instantly with trusted emergency medical services in your area, available 24/7 to ensure your safety and well-being.</p>
        </div>
    </section>

    <!-- Ambulance List Section -->
    <section class="py-5">
        <div class="container">
            <div class="row g-4">
                <% ambulances.forEach(ambulance => { %>
                    <div class="col-md-4">
                        <div class="ambulance-card">
                            <img src="<%= ambulance.AmbulanceImage || 'https://images.unsplash.com/photo-1611497319148-eda9e338f435?auto=format&fit=crop&w=300&q=80' %>" alt="Ambulance Image" class="ambulance-image" loading="lazy">
                            <div class="ambulance-details">
                                <div><i class="fas fa-user"></i><strong>Driver Name:</strong> <%= ambulance.driverName %></div>
                                <div><i class="fas fa-ambulance"></i><strong>Ambulance Name:</strong> <%= ambulance.ambulanceName %></div>
                                <div><i class="fas fa-car"></i><strong>Vehicle Number:</strong> <%= ambulance.vehicleNumber %></div>
                                <div><i class="fas fa-hospital"></i><strong>Medical Owner:</strong> <%= ambulance.driverName || 'Unknown Owner' %></div>
                                <div><i class="fas fa-signal"></i><strong>Status:</strong> 
                                    <span class="status status-<%= ambulance.status ? ambulance.status.toLowerCase() : 'offline' %>">
                                        <%= ambulance.status || 'Unknown' %>
                                    </span>
                                </div>
                            </div>
                            <a href="/user/ambulance/<%= ambulance._id %>" class="btn">Connect</a>
                        </div>
                    </div>
                <% }); %>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="row g-4">
                <div class="col-md-4">
                    <h5 class="fs-5 fw-bold mb-4">About Amwell</h5>
                    <p class="text-light">Your trusted healthcare companion providing 24/7 medical assistance and emergency services to ensure your safety and peace of mind.</p>
                </div>
                <div class="col-md-4">
                    <h5 class="fs-5 fw-bold mb-4">Quick Links</h5>
                    <ul class="list-unstyled">
                        <li class="mb-3"><a href="/about" class="text-light hover:text-white">About Us</a></li>
                        <li class="mb-3"><a href="/services" class="text-light hover:text-white">Services</a></li>
                        <li class="mb-3"><a href="/contact" class="text-light hover:text-white">Contact</a></li>
                    </ul>
                </div>
                <div class="col-md-4">
                    <h5 class="fs-5 fw-bold mb-4">Contact Info</h5>
                    <ul class="list-unstyled text-light">
                        <li class="mb-3"><i class="fas fa-phone me-2"></i>Emergency: 108</li>
                        <li class="mb-3"><i class="fas fa-envelope me-2"></i>support@amwell.com</li>
                        <li class="mt-4">
                            <div class="social-icons">
                                <a href="#" class="text-light"><i class="fab fa-facebook-f"></i></a>
                                <a href="#" class="text-light"><i class="fab fa-twitter"></i></a>
                                <a href="#" class="text-light"><i class="fab fa-instagram"></i></a>
                            </div>
                        </li>
                    </ul>
                </div>
            </div>
            <hr class="my-4 border-light">
            <div class="text-center text-light">
                <small>© 2025 Amwell. All rights reserved.</small>
            </div>
        </div>
    </footer>

    <!-- Back to Top Button -->
    <a href="#" class="back-to-top"><i class="fas fa-arrow-up"></i></a>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <!-- JavaScript for Interactivity -->
    <script>
        // Smooth Scroll
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                document.querySelector(this.getAttribute('href')).scrollIntoView({
                    behavior: 'smooth'
                });
            });
        });

        // Navbar Scroll Effect
        window.addEventListener('scroll', () => {
            const navbar = document.querySelector('.navbar');
            if (window.scrollY > 50) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });

        // Back to Top Button
        const backToTop = document.querySelector('.back-to-top');
        window.addEventListener('scroll', () => {
            if (window.scrollY > 300) {
                backToTop.classList.add('show');
            } else {
                backToTop.classList.remove('show');
            }
        });

        // Lazy Load Images (Intersection Observer)
        const images = document.querySelectorAll('img[loading="lazy"]');
        const observer = new IntersectionObserver((entries, observer) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    const img = entry.target;
                    img.src = img.dataset.src || img.src;
                    observer.unobserve(img);
                }
            });
        });
        images.forEach(img => observer.observe(img));
    </script>
</body>
</html>